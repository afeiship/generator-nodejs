# generator-fei-nodejs-runtime
> Yeoman generator for runtime nodejs base file.

## Installation
```bash
npm install -g yo
npm install -g afeiship/generator-fei-nodejs-runtime
```

Then generate your new project:

```bash
yo fei-nodejs-runtime
```
